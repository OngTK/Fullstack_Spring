1) 스프링 서버·AppStart 는 2개 이상 실행 안 됨
    - port 중복
2) 스프링의 AppStart는 모든 resources 폴더와 webapp폴더를 공유함
3) JSP는 무조건 webapp 폴더 내 자료만 사용 가능하다.
4) Front-end 코드들은 일부만 수정되면 브라우저에서 인식되지 않는 경우가 있음.
    이 경우, 일반 새로고침이 아닌 **강력 새로고침**을 실행
5) 모든 프로그램밍 언어들은 키워드(예약어)로 변수명/함수명/클래스명 선언 불가
    (1) Java는 키워드 오류를 즉시 알려줌
    (2) JS는 키워드 오류를 즉시 알려주지 않음 : Console 확인 필수